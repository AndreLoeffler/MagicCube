{% extends 'BinaerpilotenMagicBundle::index.html.twig' %}

{% block content %}
    <h1>Typ list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('typ_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('typ_show', { 'id': entity.id }) }}">anzeigen</a>
                    </li>
                    <li>
                        <a href="{{ path('typ_edit', { 'id': entity.id }) }}">bearbeiten</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
    
{% block admin_menu %}
	{{ menuButton('Neuer Typ','typ_new')|raw }}
{% endblock %}
