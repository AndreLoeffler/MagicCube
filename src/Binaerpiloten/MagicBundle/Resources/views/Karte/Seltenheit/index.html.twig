{% extends 'BinaerpilotenMagicBundle::index.html.twig' %}

{% block content %}
    <h1>Farbe list</h1>

    <table class="records_list table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Display</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('seltenheit_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{{ entity.display }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('seltenheit_show', { 'id': entity.id }) }}">anzeigen</a>
                    </li>
                    <li>
                        <a href="{{ path('seltenheit_edit', { 'id': entity.id }) }}">bearbeiten</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block admin_menu %}
	{{ menuButton('Neue Seltenheit', 'seltenheit_new')|raw }}
{% endblock %}